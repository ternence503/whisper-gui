openai-whisper==20250625
opencc-python-reimplemented>=0.1.7
edge-tts>=7.2.8
demucs>=4.0.1
# Switches SSL trust to the OS-native certificate store with zero code changes
# (applies automatically to every process using this venv, including the demucs
# subprocess). Fixes CERTIFICATE_VERIFY_FAILED when a corporate firewall/antivirus
# does SSL inspection with a self-signed root the OS trusts but Python doesn't.
pip-system-certs>=5.0
